{
  "name": "pond-wgpu",
  "private": true,
  "version": "0.0.1",
  "source": "src/index.html",
  "scripts": {
    "dev": "yarn copy-assets && parcel",
    "build": "yarn copy-assets && parcel build",
    "copy-assets": "node scripts/copyAssets.js"
  },
  "dependencies": {
    "webgpu-utils": "^2.0.2"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.68",
    "parcel": "^2.14.0"
  }
}
